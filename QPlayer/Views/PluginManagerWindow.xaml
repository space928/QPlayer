<Window x:Class="QPlayer.Views.PluginManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QPlayer.Views"
        xmlns:vm="clr-namespace:QPlayer.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:PluginManagerWindow}"
        Title="QPlayer – Plugin Manager" Height="500" Width="800" 
        Style="{DynamicResource CustomWindowStyle}" 
        Icon="{StaticResource IconImage}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Plugins.Count, StringFormat='0 Plugin(s) loaded'}" FontStyle="Italic" Margin="5" Opacity="0.7"/>
        <ListBox x:Name="PluginListBox" ItemsSource="{Binding Plugins}" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ScrollViewer DataContext="{Binding SelectedPlugin}" Grid.Row="1" Grid.Column="1" Margin="12,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <DockPanel>
                    <Image MinWidth="30"/>
                    <TextBlock Text="{Binding Name, FallbackValue='No Plugin Selected'}" FontSize="20"/>
                </DockPanel>
                <StackPanel Grid.Row="1">
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,2" Opacity="0.7">
                        <TextBlock Text="Author: "/>
                        <TextBlock Text="{Binding Author}" FontStyle="Italic"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,2" Opacity="0.7">
                        <TextBlock Text="Version: "/>
                        <TextBlock Text="{Binding Version}" FontStyle="Italic"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,8" Opacity="0.7">
                        <TextBlock Text="Assembly: "/>
                        <TextBlock Text="{Binding Assembly}" FontStyle="Italic"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Description}" TextWrapping="WrapWithOverflow" Margin="0,0,0,12"/>
                    <TextBlock Text="Registered cues: " Margin="12,0,0,4" Opacity="0.7"/>
                    <ListBox ItemsSource="{Binding CueTypes}" MaxHeight="120" Width="350" HorizontalAlignment="Left">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Image Source="{Binding Icon}" MaxHeight="20" Width="20" VerticalAlignment="Center" DockPanel.Dock="Left" Margin="0,0,8,0"/>
                                    <Label Content="{Binding Name}"/>
                                </DockPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
