<UserControl x:Class="QPlayer.Views.ProjectSettingsEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QPlayer.Views"
             xmlns:vm="clr-namespace:QPlayer.ViewModels"
             xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=vm:ProjectSettingsViewModel}">
    <StackPanel>
        <Label Content="Project Metadata" Background="{DynamicResource AREghZyBrush.Primary.3.Background.Static}" FontWeight="Bold" Margin="8,8,0,0"/>
        <DockPanel>
            <Label Content="Project Name"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=Default}" DockPanel.Dock="Right" Margin="8,2,2,2"/>
        </DockPanel>
        <DockPanel>
            <Label Content="Description"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=Default}" DockPanel.Dock="Right" Margin="8,2,2,2" Height="60" AcceptsReturn="True" TextWrapping="WrapWithOverflow"/>
        </DockPanel>
        <DockPanel>
            <Label Content="Author"/>
            <TextBox Text="{Binding Author, UpdateSourceTrigger=Default}" DockPanel.Dock="Right" Margin="8,2,2,2"/>
        </DockPanel>
        <DockPanel>
            <Label Content="Show Date"/>
            <DatePicker Text="{Binding Date}" DockPanel.Dock="Right" Margin="8,0,0,0"/>
        </DockPanel>
        <Label Content="Audio Setup" Background="{DynamicResource AREghZyBrush.Primary.3.Background.Static}" FontWeight="Bold" Margin="8,8,0,0"/>
        <DockPanel ToolTip="Sets the desired latency of the audio driver. Some drivers may ignore or impose limits on this parameter.">
            <Label Content="Audio Latency (ms)"/>
            <local:TextField ShowSpinner="True" SpinRate="0.1" MinValue="1" MaxValue="500" SpinnerType="Int" ClampValue="True" Text="{Binding AudioLatency}" DockPanel.Dock="Right" Margin="8,2,2,2"/>
        </DockPanel>
        <DockPanel>
            <Label Content="Sound Driver"/>
            <ComboBox SelectedItem="{Binding AudioOutputDriver}" ItemsSource="{Binding AudioOutputDriverValues}" DockPanel.Dock="Right" Margin="8,2,2,2"/>
        </DockPanel>
        <DockPanel>
            <Label Content="Output Device"/>
            <ComboBox SelectedIndex="{Binding SelectedAudioOutputDeviceIndex}" ItemsSource="{Binding AudioOutputDevices}" DockPanel.Dock="Right" Margin="8,2,2,2"/>
        </DockPanel>
        <Label Content="Network Setup" Background="{DynamicResource AREghZyBrush.Primary.3.Background.Static}" FontWeight="Bold" Margin="8,8,0,0"/>
        <DockPanel ToolTip="The network interface to listen to OSC messages on.">
            <Label Content="Network Adaptor" HorizontalAlignment="Left" DockPanel.Dock="Left"/>
            <ComboBox ItemsSource="{Binding NICs}" SelectedIndex="{Binding SelectedNIC}" Margin="8,2,2,2" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="The port to transmit OSC messages on (should be the same as MagicQ's OSC RX port).">
            <Label Content="OSC TX Port" HorizontalAlignment="Left" DockPanel.Dock="Left"/>
            <local:TextField Text="{Binding OSCTXPort}" Margin="8,2,2,2" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="The port to receive OSC messages on (should be the same as MagicQ's OSC TX port).">
            <Label Content="OSC RX Port" HorizontalAlignment="Left" DockPanel.Dock="Left"/>
            <local:TextField Text="{Binding OSCRXPort}" Margin="8,2,2,2" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="When checked, all OSC messages are logged to the log window.">
            <Label Content="Monitor OSC Messages" HorizontalAlignment="Left"/>
            <CheckBox IsChecked="{Binding MonitorOSCMessages}" Margin="8,2,2,2" Width="20" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalContentAlignment="Center"/>
        </DockPanel>
        <Label Content="Remote Setup" Background="{DynamicResource AREghZyBrush.Primary.3.Background.Static}" FontWeight="Bold" Margin="8,8,0,0"/>
        <DockPanel ToolTip="Allows this instance of QPlayer to control or be controlled by remote devices.">
            <Label Content="Enable Remote Control" HorizontalAlignment="Left"/>
            <CheckBox IsChecked="{Binding EnableRemoteControl}" Margin="8,2,2,2" Width="20" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="Allows this instance of QPlayer to control other instances. When this options is disabled, the OSC RX and TX ports are internally flipped to allow for communication with the host.">
            <Label Content="Is Remote Host" HorizontalAlignment="Left"/>
            <CheckBox IsChecked="{Binding IsRemoteHost}" Margin="8,2,2,2" Width="20" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="Sends the host's showfile to all remote clients whenever it is saved.">
            <Label Content="Sync Showfile On Save" HorizontalAlignment="Left"/>
            <CheckBox IsChecked="{Binding SyncShowFileOnSave}" Margin="8,2,2,2" Width="20" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalContentAlignment="Center"/>
        </DockPanel>
        <DockPanel ToolTip="The name of this node for remote control, must be unique within the network.">
            <Label Content="Node Name" HorizontalAlignment="Left"/>
            <local:TextField Text="{Binding NodeName}" Margin="8,2,2,2" DockPanel.Dock="Right"/>
        </DockPanel>
        <Label Content="Remote Nodes:" HorizontalAlignment="Left" FontStyle="Italic"/>
        <ScrollViewer Background="{DynamicResource AREghZyBrush.Primary.2.Background.Static}" 
                  BorderBrush="{DynamicResource Button.Static.Border}" BorderThickness="1"
                  Margin="2">
            <ItemsControl ItemsSource="{Binding RemoteNodes}" Height="150">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="vm:RemoteNodeViewModel">
                        <DockPanel>
                            <DockPanel.Style>
                                <Style TargetType="{x:Type DockPanel}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="#FF670303"/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter Property="Background" Value="{DynamicResource AREghZyBrush.Primary.3.Background.Static}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DockPanel.Style>
                            <Button Content="❌" Command="{Binding DataContext.RemoveRemoteNodeCommand, 
                                Mode=OneWay, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                CommandParameter="{Binding}"
                                DockPanel.Dock="Right" HorizontalAlignment="Right"
                                Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
                            <Label Content="{Binding Name}" HorizontalAlignment="Left"/>
                            <Label Content="{Binding Address}" HorizontalAlignment="Left"/>
                        </DockPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Style>
                    <Style TargetType="ItemsControl">
                        <Style.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <TextBlock Text="No remote nodes detected." VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ItemsControl.Style>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</UserControl>
