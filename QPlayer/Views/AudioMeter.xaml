<UserControl x:Class="QPlayer.Views.AudioMeter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QPlayer.Views"
             xmlns:vm="clr-namespace:QPlayer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=vm:AudioMeterViewModel}" 
             DataContextChanged="UserControl_DataContextChanged">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="18"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=Clip}" Value="true">
                        <Setter Property="Foreground" Value="#fb21"/>
                    </DataTrigger>
                </Style.Triggers>
                <!--<Setter Property="Foreground" Value="#f12f"/>-->
            </Style>
        </Grid.Resources>
        <TextBlock Text="L" VerticalAlignment="Center" Margin="4,0,0,0"/>
        <TextBlock Text="R" VerticalAlignment="Center" Margin="4,0,0,0" Grid.Row="1"/>
        <ProgressBar Minimum="-50" Maximum="0" Value="{Binding PeakL}" Height="10" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Grid.Column="1"/>
        <ProgressBar Minimum="-50" Maximum="0" Value="{Binding PeakR}" Height="10" VerticalAlignment="Top" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1"/>
        <ProgressBar Minimum="-50" Maximum="0" Value="{Binding RMSL}" Opacity="0.5" Height="10" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Grid.Column="1"/>
        <ProgressBar Minimum="-50" Maximum="0" Value="{Binding RMSR}" Opacity="0.5" Height="10" VerticalAlignment="Top" HorizontalAlignment="Stretch" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</UserControl>
