<UserControl x:Class="QPlayer.Views.ActiveCueControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QPlayer.Views" 
             xmlns:vm="clr-namespace:QPlayer.ViewModels" 
             xmlns:util="clr-namespace:QPlayer.Utilities" 
             d:DataContext="{d:DesignInstance Type=vm:DummyCueViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid MouseEnter="Grid_MouseEnter" MouseLeave="Grid_MouseLeave">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="30"/>
            <ColumnDefinition Width="1.5*" MinWidth="44"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <util:ElapsedTimeConverter x:Key="ElapsedTimeConverter"/>
        </Grid.Resources>
        <Label Content="{Binding FullQID}" ToolTip="{Binding FullQID}" MinWidth="30" Grid.Column="0" VerticalContentAlignment="Center"/>
        <local:PlaybackProgressBar MinWidth="20" Grid.Column="1" Focusable="False"/>
        <Label Content="{Binding NamePreview}" ToolTip="{Binding NamePreview}" MinWidth="50" Grid.Column="2" VerticalContentAlignment="Center"/>
        <StackPanel x:Name="TransportButtons" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Collapsed" Height="25">
            <Button Command="{Binding StopCommand}" ToolTip="Stops this cue" Focusable="False" Style="{StaticResource ButtonRoundLeft}" MinWidth="20" MaxWidth="30">
                <Image Source="{StaticResource IconStop}" Margin="2"/>
            </Button>
            <Button Command="{Binding GoCommand}" ToolTip="Restarts this cue" Focusable="False" Style="{StaticResource ButtonRoundNone}" MinWidth="20" MaxWidth="30">
                <Image Source="{StaticResource IconPlay}" Margin="2"/>
            </Button>
            <Button Command="{Binding PauseCommand}" ToolTip="Pauses this cue" Focusable="False" Style="{StaticResource ButtonRoundRight}" MinWidth="20" MaxWidth="30">
                <Image Source="{StaticResource IconPause}" Margin="2"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
